<header>
  <nav class="navbar navbar-expand-lg" id="navbar">
    <div class="container-fluid">
      <a class="navbar-brand" href="{{ path('redirect') }}"
        ><img src="{{ asset('image/afparLogoDroit.png') }}" alt="Logo Afpar"
      /></a>
      <button
        class="navbar-toggler"
        type="button"
        data-bs-toggle="collapse"
        data-bs-target="#navbarNavDropdown"
        aria-controls="navbarNavDropdown"
        aria-expanded="false"
        aria-label="Toggle navigation"
      >
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarNavDropdown">
        <ul class="navbar-nav">
          <li class="nav-item">
            <a class="nav-link" href="{{ path('user_reservation') }}"
              >Réserver mes Repas</a
            >
          </li>
          <li class="nav-item">
            <a class="nav-link" href="{{ path('user_consultation') }}"
              >Consulter mes Réservations</a
            >
          </li>
          <li class="nav-item">
            <a class="nav-link" href="{{ path('user_profil') }}"
              >Consulter mon Profil</a
            >
          </li>
          <div class="d-flex flex-row align-items-center" id="userBadge">
            <p>
              {{ app.user.userInfo.promo.section.abreviation }} :
              {{ app.user.userInfo.nom }} {{ app.user.userInfo.prenom }}
            </p>
            <a href="{{ path('logout') }}" id="deco"
              >Se déconnecter<i class="fa-solid fa-arrow-right"></i
            ></a>
          </div>
        </ul>
      </div>
    </div>
  </nav>
</header>
