<header>
  <nav class="navbar navbar-expand-lg" id="navbar">
    <div class="container-fluid">
      <a class="navbar-brand" href="{{ path('redirect') }}"
        ><img src="{{ asset('image/afparLogoDroit.png') }}" alt="Logo Afpar"
      /></a>
      <button
        class="navbar-toggler"
        type="button"
        data-bs-toggle="collapse"
        data-bs-target="#navbarNavDropdown"
        aria-controls="navbarNavDropdown"
        aria-expanded="false"
        aria-label="Toggle navigation"
      >
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarNavDropdown">
        <ul class="navbar-nav">
          <li class="nav-item dropdown">
            <a
              class="nav-link dropdown-toggle"
              href="#"
              role="button"
              data-bs-toggle="dropdown"
              aria-expanded="false"
            >
              Gérer les Menus
            </a>
            <ul class="dropdown-menu">
              <li>
                <a class="dropdown-item" href="{{ path('admin_menu_creer') }}"
                  >Créer un Menu</a
                >
              </li>
              <li>
                <a class="dropdown-item" href="{{ path('admin_consultation') }}"
                  >Consulter les Menus</a
                >
              </li>
              <li>
                <a class="dropdown-item" href="#">Bloquez la Réservation</a>
              </li>
            </ul>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="{{ path('admin_recap') }}"
              >Récapitulatif des Réservations</a
            >
          </li>
          <li class="nav-item dropdown">
            <a
              class="nav-link dropdown-toggle"
              href="#"
              role="button"
              data-bs-toggle="dropdown"
              aria-expanded="false"
            >
              Gérer les Utilisateurs
            </a>
            <ul class="dropdown-menu">
              <li>
                <a
                  class="dropdown-item"
                  href="{{ path('security_registration') }}"
                  >Créer un Utilisateur</a
                >
              </li>
              <li>
                <a class="dropdown-item" href="{{ path('admin_gestion_user') }}"
                  >Gérer un Utilisateur</a
                >
              </li>
              <li>
                <a class="dropdown-item" href="{{ path('admin_section') }}">
                  Gérer les Sections</a
                >
              </li>
            </ul>
          </li>
          <div class="d-flex flex-row align-items-center" id="userBadge">
            <p>User : Administration</p>
            <a href="{{ path('logout') }}" id="deco"
              ><p class="d-flex flex-row align-items-center">
                <span id="decoText">Se déconnecter </span>
                <i class="fas fa-arrow-right"></i></p
            ></a>
          </div>
        </ul>
      </div>
    </div>
  </nav>
</header>
